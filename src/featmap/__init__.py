"""featmap — AI-maintained feature map (MAP.md) toolkit.

No LLM calls inside: the tool only manages files, parses and validates
the map. All intelligence is provided by the user's AI assistant.
"""

__version__ = "0.1.0"

# Version of the MAP.md format this tool reads and writes.
FORMAT_VERSION = "1"
