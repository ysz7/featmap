<!-- featmap v1 -->
# Проект: <название проекта>

Замените эти строки 2–3 строками о том, что это за проект и зачем он нужен.
Это вершина пирамиды: читатель может остановиться здесь и понять суть.

<!--
Шпаргалка формата (featmap v1):
- Ровно один `#` — проект, под ним 2–3 строки описания.
- `## Слой: <имя>` — только слои: архитектурные зоны (обычно 2–5), не папки.
- `### <Имя фичи> {#kebab-id}` — только фичи, якорь уникален по файлу.
- Поля фичи, строго в этом порядке: **Что:**, **Файлы:**, **Зависит:**, **Статус:**
- **Файлы:** пути в backticks от корня репо, допустим суффикс `:строка`.
- **Зависит:** ссылки [Имя](#id) через запятую, либо — если зависимостей нет.
- **Статус:** active | wip | deprecated.
- Тело фичи ≤ 6 строк. Только «что и где», никогда «как».
- Глубже `###` заголовки запрещены.
- **Используется:** генерирует `featmap links`, руками не править.
Проверка: `featmap check`.
-->

## Слой: Пример

Замените на реальный слой: зона ответственности, а не папка.

### Пример фичи {#example-feature}

**Что:** Замените: 1–2 предложения о том, что делает фича.
**Файлы:** `src/example.py`
**Зависит:** —
**Статус:** wip
