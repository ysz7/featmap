<!-- featmap v1 -->
# Project: <project name>

Replace these lines with 2-3 lines about what this project is and why it exists.
This is the top of the pyramid: a reader can stop here and still get the gist.

<!--
Format cheat sheet (featmap v1):
- Exactly one `#` heading: the project, followed by a 2-3 line description.
- `## Layer: <name>` headings are layers only: architectural zones (usually 2-5), not folders.
- `### <Feature name> {#kebab-id}` headings are features only; anchors are unique per file.
- Feature fields, strictly in this order: **What:**, **Files:**, **Depends:**, **Status:**
- **Files:** backtick paths relative to the repo root; an optional `:line` suffix is allowed.
- **Depends:** comma-separated [Name](#id) links, or — when there are no dependencies.
- **Status:** active | wip | deprecated.
- Feature body is at most 6 lines. Only "what and where", never "how".
- Headings deeper than `###` are forbidden.
- **Used by:** is generated by `featmap links`; never edit it by hand.
Validate with: `featmap check`.
-->

## Layer: Example

Replace with a real layer: a zone of responsibility, not a folder.

### Example feature {#example-feature}

**What:** Replace: 1-2 sentences about what the feature does.
**Files:** `src/example.py`
**Depends:** —
**Status:** wip
